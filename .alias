#!/bin/bash

md() {
    mkdir -p $1
    cd $1
}
alias mkdir='mkdir -p'

alias lsd='ls -l|grep "^d"'
alias ll='ls -laF'
alias lt='ls -laFt'
alias lth='ls -laFt|head -20'

alias myls="ls -lsaSr --block-size=M"

alias df='df -h'

alias grep='grep --color=auto'

alias poweroff='sudo shutdown -h now'

alias install='sudo apt-get install'

alias password='openssl rand -base64 12' 

# Pretty print the path
alias path='echo $PATH | tr -s ":" "\n"'

# alias gitlog='log --graph --pretty=format:'%Cred%h%Creset %C(cyan)%an%Creset -%C(blue)%d%Creset %s %Cgreen(%cr)%Creset' --abbrev-commit --date=relative
